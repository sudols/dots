(deflisten
  bluetooth
  :initial "{\"icon\":\"󰂯\",\"status\":\"on\",\"connected\":false}" `./bar/scripts/bluetooth.sh`
)

(defwidget bluetooth []
  (eventbox
    :cursor "pointer"
    :onrightclick "hyprctl dispatch exec blueman-manager"
    :onclick "./bar/popup-menus/scripts/toggle-bt.sh"
    (label
      :text "${bluetooth.icon}"
      :class "bt-i"
    )
  )
)
